% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brapi_germplasm.R
\name{get_synonyms_from_germplasm_names}
\alias{get_synonyms_from_germplasm_names}
\title{Get the synonyms of a set of accessions, keyed by primary germplasm name}
\usage{
get_synonyms_from_germplasm_names(
  germplasm_names,
  brapi_connection,
  batch_size = 500L,
  pause = 0,
  max_retries = 3L,
  verbose = FALSE
)
}
\arguments{
\item{germplasm_names}{Character vector of primary germplasm names to look up.}

\item{brapi_connection}{A BrAPI connection object, typically from
\code{BrAPI::createBrAPIConnection()}, with a \code{$search()} method.}

\item{batch_size}{Integer; maximum number of names per BrAPI request
(default 500).}

\item{pause}{Numeric; seconds to wait between batches, to avoid tripping the
server's rate limit (default 0).}

\item{max_retries}{Integer; attempts per batch on a transient failure (e.g. an
HTTP 403), with exponential backoff between attempts (default 3).}

\item{verbose}{Logical; if \code{TRUE}, print per-batch progress messages.}
}
\value{
A tibble with one row per (primary, synonym) pair and columns
\code{primary_name} and \code{synonym}. Accessions with no synonyms (or that
are not found) contribute no rows.
}
\description{
Looks up germplasm records by their \strong{primary} \code{germplasmName} and
returns the synonyms recorded for each. This matters when an accession was
genotyped under a \emph{preliminary} experimental line name that was later demoted
to a synonym after a \emph{final} name was assigned: a VCF (or any other data
source) may carry the sample under the synonym while downstream analysis keys
on the primary name.
}
\details{
Queries the BrAPI \code{/search/germplasm} endpoint with
\code{germplasmNames}, batched to keep each request small, and collates the
synonyms returned for each matched record.

Synonyms are returned by BrAPI either as bare strings or as objects
with a \code{synonym} field; both shapes are handled. A batch that keeps
failing after \code{max_retries} is warned about and skipped (its accessions
simply contribute no synonym rows) rather than aborting the whole call.
}
\examples{
\dontrun{
brapi_conn <- BrAPI::createBrAPIConnection("wheat.triticeaetoolbox.org", is_breedbase = TRUE)
get_synonyms_from_germplasm_names(c("ACC_ONE", "ACC_TWO"), brapi_conn)
}

}
